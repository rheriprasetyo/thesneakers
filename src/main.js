import { createApp } from 'vue'
import App from './App.vue'
import router from './router'
// tambahkan bootstrap di sini
import 'bootstrap' 


createApp(App).use(router).use(router).use(router).use(router).mount('#app')



